spring:
  application:
    name: cloudproviderconsulpayment8003
  cloud:
    consul:
      discovery:
        service-name: ${spring.application.name}
      host: localhost
      port: 8500
server:
  port: 8003